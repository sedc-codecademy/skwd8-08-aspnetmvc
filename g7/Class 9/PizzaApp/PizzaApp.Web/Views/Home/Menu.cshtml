@model List<PizzaVM>
@{

}

<h1>Pizza Menu</h1>

<div class="container">
    @{
        for (int i = 0; i < Model.Count; i++)
        {
            @Html.Partial("_PizzaItem", Model[i]);
        }
    }
</div>