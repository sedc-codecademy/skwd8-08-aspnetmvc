@model List<SizeViewModel>

<h4>Size List</h4>


<a asp-controller="Size" asp-action="Add" class="btn btn-block float-right col-2">Add</a>
<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @for (int i = 0; i < Model.Count; i++)
    {
        <tr>
            <th scope="row">@(i + 1)</th>
            <td>@Model[i].Name</td>
            <td>@Model[i].Description</td>
            <td>
                <a asp-controller="Size" asp-action="Details" asp-route-id="@Model[i].Id" class="btn btn-info">Details</a>
                <a asp-controller="Size" asp-action="Edit" asp-route-id="@Model[i].Id" class="btn btn-warning">Edit</a>
                <a asp-controller="Size" asp-action="Delete" asp-route-id="@Model[i].Id" class="btn btn-danger">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>